import 'package:flutter/material.dart';

class EasyEarningHistoryScreen extends StatelessWidget {
  const EasyEarningHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Easy Earning History Screen'),
    );
  }
}
