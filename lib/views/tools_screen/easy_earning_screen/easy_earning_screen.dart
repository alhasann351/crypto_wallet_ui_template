import 'package:flutter/material.dart';

class EasyEarningScreen extends StatelessWidget {
  const EasyEarningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Easy Earning'),
      ),
    );
  }
}
