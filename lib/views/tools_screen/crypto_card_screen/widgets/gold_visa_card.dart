import 'package:flutter/material.dart';

class GoldVisaCard extends StatelessWidget {
  const GoldVisaCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Gold visa card')));
  }
}
