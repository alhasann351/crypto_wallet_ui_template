import 'package:flutter/material.dart';

class CryptoGiveawayScreen extends StatelessWidget {
  const CryptoGiveawayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Crypto Giveaway')),
    );
  }
}
