import 'package:flutter/material.dart';

class TipJarScreen extends StatelessWidget {
  const TipJarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Tip Jar')),
    );
  }
}
