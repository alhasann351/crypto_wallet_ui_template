import 'package:flutter/material.dart';

class BatchPaymentsScreen extends StatelessWidget {
  const BatchPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Batch Payments')),
    );
  }
}
